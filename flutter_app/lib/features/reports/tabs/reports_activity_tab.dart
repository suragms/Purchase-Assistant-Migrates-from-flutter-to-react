import 'package:flutter/material.dart';

import '../presentation/widgets/reports_movement_tab.dart';

/// Activity tab wrapper (timeline by date).
class ReportsActivityTab extends StatelessWidget {
  const ReportsActivityTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const ReportsMovementTab();
  }
}
